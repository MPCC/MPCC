define(['jquery', 'underscore', 'backbone'],
    function ($, _, backbone) {

        return {
            init: function () {
                alert('The app was initialized');
            }
        };
    });